# Copilot Instructions

## Pokyny k projektu
- In the PropertyMapper project, always use explicit types instead of `var`, `is not null` instead of `!= null`, `is null` instead of `== null`, and add `.ConfigureAwait(false)` to all async awaited calls.